const products=[{name:'SSH Starter',desc:'Untuk browsing ringan dan kebutuhan harian.',price:15000,period:'7 hari',location:'Singapore',featured:false,features:['1 perangkat aktif','Bandwidth unmetered','Aktivasi instan']},{name:'SSH Premium',desc:'Performa stabil untuk streaming dan kerja.',price:35000,period:'30 hari',location:'Indonesia',featured:true,features:['2 perangkat aktif','Prioritas server','Support 24/7']},{name:'SSH Pro',desc:'Paket panjang untuk penggunaan lebih serius.',price:85000,period:'90 hari',location:'Japan',featured:false,features:['3 perangkat aktif','Multi-lokasi','Garansi penggantian']}];
const money=n=>new Intl.NumberFormat('id-ID',{style:'currency',currency:'IDR',maximumFractionDigits:0}).format(n);let balance=Number(localStorage.getItem('sshstore_balance')||0);
const grid=document.querySelector('#productGrid');grid.innerHTML=products.map((p,i)=>`<article class="product-card ${p.featured?'featured':''}">${p.featured?'<span class="tag">PALING POPULER</span>':''}<div class="eyebrow">${p.location.toUpperCase()} • ${p.period.toUpperCase()}</div><h3>${p.name}</h3><p class="desc">${p.desc}</p><div class="price">${money(p.price)} <small>/ paket</small></div><ul>${p.features.map(f=>`<li>${f}</li>`).join('')}</ul><button class="btn ${p.featured?'btn-primary':'btn-outline'} full buy-btn" data-product="${i}">Beli Sekarang</button></article>`).join('');
function updateBalance(){document.querySelector('#headerBalance').textContent=`Saldo ${money(balance)}`}updateBalance();
const openModal=id=>{const el=document.getElementById(id);el.classList.add('open');el.setAttribute('aria-hidden','false')};const closeModal=el=>{el.closest('.modal').classList.remove('open');el.closest('.modal').setAttribute('aria-hidden','true')};document.querySelectorAll('[data-open]').forEach(b=>b.addEventListener('click',()=>openModal(b.dataset.open)));document.querySelectorAll('[data-close]').forEach(b=>b.addEventListener('click',()=>closeModal(b)));document.querySelectorAll('.modal').forEach(m=>m.addEventListener('click',e=>{if(e.target===m)m.classList.remove('open')}));
document.querySelector('#loginForm').addEventListener('submit',e=>{e.preventDefault();alert('Prototype: login member siap dihubungkan ke backend.');document.querySelector('#loginModal').classList.remove('open')});document.querySelector('#registerLink').addEventListener('click',e=>{e.preventDefault();alert('Prototype: halaman registrasi member akan ditambahkan pada tahap backend.')});
let selected=25000;document.querySelectorAll('.amount-grid button').forEach(b=>b.addEventListener('click',()=>{selected=Number(b.dataset.amount);document.querySelectorAll('.amount-grid button').forEach(x=>x.classList.remove('selected'));b.classList.add('selected')}));document.querySelector('#manualTopup').addEventListener('click',()=>{alert(`Prototype: invoice top up ${money(selected)} siap dihubungkan ke payment gateway atau verifikasi manual.`);closeModal(document.querySelector('#manualTopup'))});
document.querySelectorAll('.buy-btn').forEach(b=>b.addEventListener('click',()=>{const p=products[Number(b.dataset.product)];if(balance<p.price){alert(`Saldo belum cukup. Tambahkan saldo minimal ${money(p.price-balance)}.`);openModal('topupModal');return}balance-=p.price;localStorage.setItem('sshstore_balance',balance);updateBalance();alert(`Pembelian ${p.name} berhasil. Detail akun akan tampil di dashboard member.`)}));
