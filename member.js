const drawer=document.querySelector('#drawer'),scrim=document.querySelector('#scrim');const open=()=>{drawer.classList.add('open');scrim.classList.add('show')};const close=()=>{drawer.classList.remove('open');scrim.classList.remove('show')};document.querySelector('#menuToggle').addEventListener('click',open);document.querySelector('#menuClose').addEventListener('click',close);scrim.addEventListener('click',close);document.querySelector('#logout').addEventListener('click',()=>alert('Prototype: logout member siap dihubungkan ke backend.'));const panels=document.querySelectorAll('.member-panel');function select(name){document.querySelectorAll('.nav-item').forEach(x=>x.classList.toggle('active',x.dataset.panel===name));panels.forEach(x=>x.classList.toggle('active',x.id===`panel-${name}`));close()}document.querySelectorAll('[data-panel]').forEach(x=>x.addEventListener('click',e=>{e.preventDefault();select(x.dataset.panel)}));document.querySelectorAll('[data-panel-link]').forEach(x=>x.addEventListener('click',e=>{e.preventDefault();select(x.dataset.panelLink)}));

const productButtons=document.querySelectorAll('#panel-products .btn');productButtons.forEach(button=>button.addEventListener('click',()=>{window.location.href='index.html#products'}));
const walletButtons=document.querySelectorAll('#panel-wallet .btn');walletButtons[0]?.addEventListener('click',()=>alert('Top Up Otomatis: payment gateway akan dibuka setelah backend dan gateway dikonfigurasi.'));walletButtons[1]?.addEventListener('click',()=>alert('Top Up Manual: instruksi rekening/QRIS dan upload bukti akan dibuka setelah backend tersedia.'));
const voucherInput=document.querySelector('#panel-vouchers input');document.querySelector('#panel-vouchers .btn')?.addEventListener('click',()=>{const code=voucherInput.value.trim();alert(code?`Voucher ${code} akan diverifikasi oleh backend.`:'Masukkan kode voucher terlebih dahulu.')});document.querySelector('.referral-code button')?.addEventListener('click',async()=>{try{await navigator.clipboard.writeText('SSHSTORE-MEMBER');alert('Kode referral berhasil disalin.')}catch{alert('Kode referral: SSHSTORE-MEMBER')}});
document.querySelector('#panel-support .btn')?.addEventListener('click',()=>alert('Form tiket support akan aktif setelah backend tiket dikonfigurasi.'));document.querySelector('#panel-profile .btn')?.addEventListener('click',()=>alert('Pengaturan profil dan keamanan akan aktif setelah autentikasi backend dikonfigurasi.'));
