const form=document.querySelector('#setupForm'),error=document.querySelector('#setupError');if(localStorage.getItem('sshstore_admin_initialized')==='true')location.replace('admin.html');form.addEventListener('submit',e=>{e.preventDefault();const data=Object.fromEntries(new FormData(form));if(data.password.length<12)return error.textContent='Password minimal 12 karakter.';if(data.password!==data.confirmPassword)return error.textContent='Ulangi password belum sama.';localStorage.setItem('sshstore_admin_initialized','true');localStorage.setItem('sshstore_admin_profile',JSON.stringify({name:data.name,email:data.email,createdAt:new Date().toISOString()}));location.replace('admin.html')});
